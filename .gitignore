node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.playwright-cli/

# ── 构建产物：APK 安装包 ────────────────────────────────
# 发版时会把 Android 打好的包拷到 public/ 供下载（体积 ~16MB）。
# 不忽略的话每个版本的二进制都会永久留在 git 历史里，仓库只增不减。
# 需要它随站点发布时，在本地拷入即可，不必进版本库。
public/*.apk
